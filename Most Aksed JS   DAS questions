1.Convert a string to Title Case / Camel Case.

solve ===>>
 let str = "mujhe camale case me kro"
output="Mujhe Camale Case Me Kro"


let str2 = str.split(" ")
let result =""

for(let i=0;i<str2.length;i++){
    let word = str2[i][0].toUpperCase() + str2[i].slice(1).toLowerCase()
     result += word + " "
}
console.log(result)
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
2.Count frequency of each character in a string.

solve ====>>>>
let str = "har har mahadev"
let freq ={}

for(let i =0;i<str.length;i++){
    if(str[i]==" "){
        continue;
    }
    freq[str[i]] = (freq[str[i]]||0) + 1
}
console.log(freq)
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
3..Check if a string is a rotation of another.

solve======>
let str1 ="radhe"
let str2 ="ehrad"

let doubled = str1 + str2 
if(doubled.includes(str2)){
    console.log("it is rotetions")
}
else{
    console.log("it is not rootentions")
}

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


4.Find the longest substring without repeating characters. 
const str = "pwwkew";

let result = "";
let longest = "";

for (let i = 0; i < str.length; i++) {

    let index = result.indexOf(str[i]);

    if (index !== -1) {
        result = result.slice(index + 1);
    }

    result += str[i];

    if (result.length > longest.length) {
        longest = result;
    }
}

console.log(longest);

????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
c

????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
6.Check if two strings are anagrams.?

????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????

7.Find the first non-repeating character.
solveee =>>>>>>>>>>>>>>




8.Rotate an array by k positions.
solve------>>>>
    let num = [1,2,3,4,5,6,7,8,9]
let k =2
let n = num.length
let result =[]

for(let i=0;i<n;i++){
    let newIndex = (i+k)%n 
    result[newIndex] = num[i]
    
}
    console.log(result);

lef to right
    let num = [1,2,3,4,5,6,7,8,9]
let k =2
let n = num.length
let result =[]

for(let i=0;i<n;i++){
    let newIndex = (i-k+n)%n 
    result[newIndex] = num[i]
    
}
    console.log(result);


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
9.Remove duplicates from an array.
--->
    let num = [1,2,2,3,4,5,4,6,6,]
let unique =[]
for(let i =0; i<num.length; i++){
    let isDuplicate = false;
    for(let j=0;j<unique.length;j++){
        if(num[i]=== unique[j]){
            isDuplicate =true
            break;
        }
        }
        if(!isDuplicate){
            unique[unique.length] =num[i]
    }
}
console.log(unique)

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

10. Find the missing number in an array (1 to n).

solveeee----->>>>>>>>
let num = [1,2,3,4,5,6]

for(let i=0;i<num.length-1;i++){
    if(num[i+1] -num[i]!==1){
        console.log("missing number is", num[i]+1)
        break;
    }
}

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


Reverse a string without using reverse().

Check if a string is a palindrome.

Count vowels and consonants in a string.

Find the first non-repeating character.

Check if two strings are anagrams.

Compress a string (Run Length Encoding).

Find the longest substring without repeating characters.

Check if a string is a rotation of another.

Count frequency of each character in a string.

Convert a string to Title Case / Camel Case


Reverse an array without using built-in methods.

Find the maximum and minimum element in an array.

Find the second largest / second smallest element.
let num =[100,45,30,40,50,53]


let  largestNumber =0
let  SecoundLargest =0

for(let i=0;i<num.length;i++){
     if(num[i]>largestNumber){
        SecoundLargest =largestNumber
        largestNumber =num[i]

     }
     else{
        if(largestNumber>num[i] && largestNumber>SecoundLargest){
            SecoundLargest = num[i]
        }
     }
}
console.log(largestNumber)
console.log(SecoundLargest)




Check if an array is sorted or not.

Rotate an array by k positions.

Find duplicate elements in an array.

Remove duplicates from an array.

Find the missing number in an array (1 to n).

Merge two sorted arrays into one sorted array.

Kadane’s Algorithm → Find maximum subarray sum.






const users = [
//   { name: "Ritesh", city: "Indore" },
//   { name: "Lalit", city: "Bhopal" },
//   { name: "Rohan", city: "Indore" }
// ];


// let result = users.reduce((acc,curr)=>{
//     acc[curr.city] =[...(acc[curr.city]||[]),curr]
//     return acc
// },{})

// console.log(result)

let arr =[1,[2,3,[4,5],[6,7]]]

function fltat(arr){
    let result =[]
    function rec(newArr){
        for(let i=0;i<newArr.length;i++){
            if(Array.isArray(newArr[i])){
               
        rec(newArr[i]);
        }
        else{
            result =  [...result,newArr[i]]
        }
        
    }
}
    rec(arr)
    console.log(result)
}
fltat(arr)

let arr = [1,2,3,0,1,0]

let index = 0
let result = []

// Add non-zero elements
for(let i=0;i<arr.length;i++){

    if(arr[i] !== 0){
        result[result.length] = arr[i]
    }

}

// Count zeros
for(let i=0;i<arr.length;i++){

    if(arr[i] === 0){
        result[result.length] = 0
    }

}

console.log(result)
>>


let str = "aaabbbcccaaadd"

let result  = ""
for(let i =0;i<str.length;i++){
    
    if(str[i]!==str[i+1]){
        result += str[i]
    }
}
console.log(result)

const users = [
  { id: 1, name: "Ritesh" },
  { id: 2, name: "Rahul" },
  { id: 1, name: "Ritesh" },
  { id: 3, name: "Amit" },
  { id: 2, name: "Rahul" }
];
let result =[]
for(let i=0;i<users.length;i++){
     let isDuplicate =false
    for(let j=0;j<result.length;j++){
        if(users[i].id ===result[j].id && result[j].name===users[i].name){
            isDuplicate=true
            continue;
            break;
                 
        }
    }
    if(!isDuplicate){
        result[result.length] = users[i]
    }
    
}

##########################################################################
first nonrepeat crecter return you want 
